"# groovy_holaThomas" 

- Un ejemplo de una web aplicación sensilla con groovy.
- se arranca con "grails" y después "run-app" y se usa a la url http://localhost:8080.

La aplicación mostra una pagina per defecto y una otra generada con el controller HelloController. 